query {
  Page(page: 1, perPage: 10) {
    airingSchedules(sort: TIME_DESC, notYetAired: false) {
      nodes {
        media {
          id
          title {
            userPreferred
          }
          coverImage {
            large
          }
        }
        episode
        airingAt
      }
    }
  }
}
