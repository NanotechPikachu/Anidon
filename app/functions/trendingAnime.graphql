query {
  Page(page: 1, perPage:20) {
    media(isAdult: false, sort: TRENDING_DESC, type: ANIME) {
      id
      title {
        userPreferred
      }
      coverImage {
        large
      }
    }
  }
}
